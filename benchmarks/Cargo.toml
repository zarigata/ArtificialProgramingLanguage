[package]
name = "vez_benchmarks"
version = "1.0.0"
edition = "2021"
authors = ["VeZ Team <team@vez-lang.org>"]
description = "VeZ Compiler and Runtime Benchmarks"
license = "MIT"

[[bin]]
name = "vez-bench"
path = "src/main.rs"

[dependencies]
colored = "2.1"
anyhow = "1.0"
